# YouTube API Configuration
YOUTUBE_API_KEY=your_youtube_api_key_here
YOUTUBE_CHANNEL_ID=your_youtube_channel_id_here

# Sanity is public-read. Do not put a Sanity token in NEXT_PUBLIC_* — it ships to the browser.
# Editor token for chat writes + the daily video cron only (server-side).
SANITY_API_TOKEN=your_sanity_write_token_here
# Vercel sends this as Authorization: Bearer … on cron. Random string is fine.
CRON_SECRET=your_cron_secret_here

# Mapbox Configuration
NEXT_PUBLIC_MAPBOX_TOKEN=your_mapbox_token_here
